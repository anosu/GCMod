# Generated by ModEngineering.
name: build
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}
jobs:
  build:
    uses: anosu/ModEngineering/.github/workflows/consumer.yml@350e407a5aca296a47b7a5935e884fce77c6ea5c
    with:
      kind: pc
