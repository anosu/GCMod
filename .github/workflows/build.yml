# Generated by ModEngineering.
name: build
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}
jobs:
  build:
    uses: anosu/ModEngineering/.github/workflows/consumer.yml@0b038f40808e29dcd31c2de5df14f2f37953ca34
    with:
      kind: pc
      extension: false
